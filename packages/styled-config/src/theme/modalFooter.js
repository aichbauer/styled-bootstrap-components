import { colors } from './colors';
import { utils } from './utils';

const modalFooter = {
  colors: {
    default: {
      borderColor: colors.borderCard,
    },
  },
  padding: {
    default: '1rem',
  },
  border: utils.border,
};

export { modalFooter };
