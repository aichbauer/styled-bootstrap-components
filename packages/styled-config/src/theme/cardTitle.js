import { colors } from './colors';

const cardTitle = {
  colors: {
    default: {
      colorMuted: colors.gray,
    },
  },
  margin: {
    bottom: '0.75rem',
    subtitleTop: '-0.375rem',
    subtitleBottom: '0',
  }
};

export { cardTitle };
