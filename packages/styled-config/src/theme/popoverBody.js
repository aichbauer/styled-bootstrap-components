import { colors } from './colors';

const popoverBody = {
  colors: {
    default: {
      color: colors.gray900,
    },
  },
  padding: {
    default: '0.5rem 0.75rem',
  },
};

export { popoverBody };
