import { colors } from './colors';

const popoverArrow = {
  colors: {
    default: {
      borderColor: colors.popoverArrowBorderColor,
      borderColorWhite: colors.white,
    },
  },
};

export { popoverArrow };
