import { theme } from './theme';
import {
  getBorder,
  borderRadius,
  getBoxShadow,
  getColor,
  fontFamily,
  fontSize,
  fontWeight,
  height,
  getMargin,
  getPadding,
  getBreakpointSize,
  getConcreteBreakpointSize,
  width,
} from './utils';

export {
  theme,
  // utils
  getBorder,
  borderRadius,
  getBoxShadow,
  getColor,
  fontFamily,
  fontSize,
  fontWeight,
  height,
  getMargin,
  getPadding,
  getBreakpointSize,
  getConcreteBreakpointSize,
  width,
};
