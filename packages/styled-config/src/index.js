import { theme } from './theme';
import {
  getBorder,
  getBorderRadius,
  getBoxShadow,
  getColor,
  getFontFamily,
  getFontSize,
  getFontWeight,
  height,
  getMargin,
  getPadding,
  getBreakpointSize,
  getConcreteBreakpointSize,
  getWidth,
} from './utils';

export {
  theme,
  // utils
  getBorder,
  getBorderRadius,
  getBoxShadow,
  getColor,
  getFontFamily,
  getFontSize,
  getFontWeight,
  height,
  getMargin,
  getPadding,
  getBreakpointSize,
  getConcreteBreakpointSize,
  getWidth,
};
