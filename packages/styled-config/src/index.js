import { theme } from './theme';
import {
  getBorder,
  getBorderRadius,
  getBoxShadow,
  getColor,
  getFontFamily,
  getFontSize,
  getFontWeight,
  getHeight,
  getMargin,
  getPadding,
  getConfigProperty,
  getBreakpointSize,
  getConcreteBreakpointSize,
  getWidth,
} from './utils';

export {
  theme,
  // utils
  getBorder,
  getBorderRadius,
  getBoxShadow,
  getColor,
  getFontFamily,
  getFontSize,
  getFontWeight,
  getHeight,
  getMargin,
  getPadding,
  getConfigProperty,
  getBreakpointSize,
  getConcreteBreakpointSize,
  getWidth,
};
